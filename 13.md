# Server state

- React -> SWR y Rea14makct-query 
- Vue -> ¿Composition API?
- Svelte -> 

## react-query 

npm library
couple of hooks
5kb 
ready to use out-of-the box
```js
import axios from "axios";
import { useQuery } from 'react-query'

const fetchTodos = () => axios.get('/api/todos').then(res => res.data)

export default function useTodos() {
  return useQuery('todos', fetchTodos)
}

// src/pages/index.js
function Index(){
    const { isLoading, data, refetch } = useTodos();
    return (
        <ul>
        {data?.map(item => (<li>{item.title}</li>))}
        </ul>
    )
}
```





# Client State

Tu Framework!

React es un manejador de estado

> sy en general tu framework de elección debería ser suficiente
